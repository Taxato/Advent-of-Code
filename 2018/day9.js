import { time } from "../helper.js";
const startTime = process.hrtime();

function marbleMania(numPlayers, lastMarbleWorth) {
	let turn = 1;
	let index = 0;
	let marbles = [0];
}

function marbleTurn(turn,index,marbles) {
    if()
}

time(startTime);
